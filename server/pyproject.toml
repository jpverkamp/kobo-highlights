[project]
name = "kobo-highlights-server"
version = "0.1.0"
description = "Local server for Kobo highlights"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "jinja2>=3.1",
  "markdown>=3.6",
  "python-multipart>=0.0.26",
]

[tool.uv]
package = false
